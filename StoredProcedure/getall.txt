 CREATE OR REPLACE FUNCTION get_all_users()
RETURNS TABLE (
    Id UUID,
    Username VARCHAR(50),
    Password VARCHAR(50)
) AS
$$
BEGIN
    RETURN QUERY SELECT users.Id, users.Username, users.Password FROM users;
END;
$$
LANGUAGE plpgsql;

SELECT * FROM get_all_users();